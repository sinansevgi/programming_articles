<div class="alert alert-light m-4" role="alert">
  <h4 class="alert-heading">Your Articles</h4>
  <hr>
  <p class="mb-0">You can click to the pen icon next to author name to edit your articles.</p></div>
<div class="row">

  <% @user.all_articles.each do |article| %>
    <%= render "article", article: article %>
  <% end %>
</div>
