<nav class="navbar navbar-expand-lg navbar-light fixed-top bg-light" aria-label="Main navigation">
  <div class="container-fluid flex-lg-row flex-row-reverse">
    <%= link_to root_path, class: 'navbar-brand m-auto' do %>
      Programming Articles<i class="fas fa-code text-primary ml-1"></i>
    <% end %>
    <button class="navbar-toggler p-0 border-0" type="button" data-bs-toggle="offcanvas" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse offcanvas-collapse" id="navbarsOffcanvas">

      <ul class="navbar-nav m-auto mb-2 mb-lg-0 flex-grow-0">
        <li class="nav-item">
          <%= nav_link("Home", categories_path) %>
        </li>
        <% Category.all_categories.each do |category| %>
          <%= nav_link(category.name, category_path(category)) %>
        <% end %>
      </ul>
      <div>
        <%= user_buttons %>
        <i class="fas fa-search"></i>
      </div>
    </div>
  </div>
</nav>
